import { PokeDirective } from './poke.directive';

describe('PokeDirective', () => {
  it('should create an instance', () => {
    const directive = new PokeDirective();
    expect(directive).toBeTruthy();
  });
});
